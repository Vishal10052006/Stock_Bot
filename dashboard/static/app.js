var S={data:null,view:"overview",inspect:null};
function esc(v){return String(v==null?"":v).replace(/[&<>"']/g,function(c){return({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c])})}
function get(p){return fetch(p,{cache:"no-store"}).then(function(r){if(!r.ok)throw Error(r.status);return r.json()})}
function st(s){return '<span class="state '+esc(s)+'">● '+esc(s)+'</span>'}
function panel(t,b,c){return '<section class="panel '+(c||"")+'"><h2>'+t+'</h2>'+b+'</section>'}
function agent(a){return '<article class="agent" onclick="inspectAgent(\''+esc(a.agent_id)+'\')"><div class="head"><span>'+esc(a.name)+'</span>'+st(a.status)+'</div><div class="sub">'+esc(a.role)+'</div><div class="sub">CONTRACT: '+esc(a.output_contract)+'</div><div class="sub">EVENTS: '+a.event_count+'</div><div class="sub">'+(a.last_event?esc(a.last_event.event_type):"WAITING FOR TELEMETRY")+'</div></article>'}
function pipe(){return S.data.pipeline.map(function(p){return '<div class="stage"><small>'+p.stage_id+'</small><b>'+esc(p.name)+'</b>'+st(p.status)+'<div class="sub">'+esc(p.detail)+'</div></div>'}).join('<div style="color:var(--c);text-align:center">→</div>')}
function ev(){if(!S.data.events.length)return '<div class="notice">No telemetry recorded yet. Configure the runtime to write the shared MonitoringJournal.</div>';return S.data.events.map(function(e){return '<div class="event"><span>'+esc(e.timestamp).slice(11,23)+'</span><span class="source">'+esc(e.source)+'</span><span>'+esc(e.event_type)+'</span><span class="sev">'+esc(e.severity)+'</span></div>'}).join("")}
function overview(){var s=S.data.summary,l=S.data.learning;return '<div class="grid">'+panel("01 | PIPELINE TRACE / CURRENT CASCADE",'<div class="pipeline">'+pipe()+'</div>','full')+panel("02 | AGENT ACTIVE ROSTER",'<div class="cards">'+S.data.agents.map(agent).join("")+'</div>')+panel("03 | TELEMETRY STREAM",'<div class="sub">REAL EVENTS • SYNTHETIC REPLAY MUST BE EXPLICIT</div>'+ev())+panel("04 | CONTROLLED SELF-LEARNING & MODEL EVOLUTION",'<div class="metric-grid">'+l.stages.map(function(x){return '<div class="metric"><b>'+x.count+'</b><small>'+esc(x.name)+'</small></div>'}).join("")+'</div><div class="sub">AUTHORITY: NON-AUTHORITATIVE</div>','full')+panel("05 | SYSTEM STATE",'<div class="metric-grid"><div class="metric"><b>'+s.registered_agents+'</b><small>AGENTS</small></div><div class="metric"><b>'+s.events+'</b><small>EVENTS</small></div><div class="metric"><b class="green">ENFORCED</b><small>CAUSALITY</small></div><div class="metric"><b class="red">LOCKED</b><small>LIVE TRADING</small></div></div>','full')+'</div>'}
function agents(){return panel("AGENT REGISTRY",'<div class="cards">'+S.data.agents.map(agent).join("")+'</div>')}
function pipeline(){return panel("PIPELINE TRACE",'<div class="pipeline">'+pipe()+'</div><div class="notice">Missing telemetry is WAITING, never fabricated as ACTIVE.</div>')}
function models(){var m=S.data.models;if(!m.length)return panel("MODEL OBSERVABILITY",'<div class="notice">No ModelRegistry object is attached. This is a real empty state.</div>');var h='<table><tr><th>VERSION</th><th>FAMILY</th><th>FEATURES</th><th>DATASET</th><th>STATUS</th></tr>';m.forEach(function(x){h+='<tr><td>'+esc(x.model_version)+'</td><td>'+esc(x.model_family)+'</td><td>'+esc(x.feature_version)+'</td><td>'+esc(x.dataset_version)+'</td><td>'+esc(x.approval_status)+'</td></tr>'});return panel("MODEL OBSERVABILITY",h+'</table>')}
function health(){return panel("SYSTEM HEALTH",'<div class="metric-grid"><div class="metric"><b class="green">ENFORCED</b><small>CAUSALITY</small></div><div class="metric"><b class="red">LOCKED</b><small>LIVE BROKER</small></div><div class="metric"><b>YES</b><small>PAPER MODE</small></div><div class="metric"><b>'+S.data.health.event_count+'</b><small>JOURNAL EVENTS</small></div></div><div class="json">'+esc(JSON.stringify(S.data.health.monitoring||{},null,2))+'</div>')}
function learning(){return panel("CONTROLLED SELF-LEARNING & MODEL EVOLUTION",'<div class="metric-grid">'+S.data.learning.stages.map(function(x){return '<div class="metric"><b>'+x.count+'</b><small>'+esc(x.name)+'</small></div>'}).join("")+'</div><div class="notice">Learning telemetry is observational; promotion remains governed by existing validation and registry gates.</div>')}
function decisions(){var d=S.data.decisions||[];if(!d.length)return panel("DECISION INSPECTOR",'<div class="notice">No correlated decision trace is present in the MonitoringJournal.</div>');return panel("DECISION INSPECTOR",d.map(function(x){return '<article class="agent" onclick="inspectDecision(\''+esc(x.decision_id)+'\')"><div class="head"><b>'+esc(x.decision_id)+'</b><span>'+x.event_count+' events</span></div><div class="sub">'+esc(x.timestamp)+'</div></article>'}).join(""))}
function events(){return panel("EVENT LOG",ev())}
function inspectAgent(id){S.inspect={title:id,data:S.data.agents.find(function(x){return x.agent_id===id})};render()}
function inspectDecision(id){S.inspect={title:"DECISION / "+id,data:(S.data.decisions||[]).find(function(x){return x.decision_id===id})};render()}
function render(){if(!S.data)return;document.getElementById("env").textContent=S.data.environment;document.getElementById("registered").textContent=S.data.summary.registered_agents+" AGENTS REGISTERED";document.getElementById("agent-count").textContent=S.data.summary.registered_agents;document.getElementById("active-count").textContent=S.data.summary.active;document.getElementById("waiting-count").textContent=S.data.summary.waiting;document.getElementById("locked-count").textContent=S.data.summary.locked;document.getElementById("telemetry").textContent=S.data.summary.events?"LIVE":"WAITING";var f={overview:overview,agents:agents,pipeline:pipeline,decisions:decisions,models:models,health:health,learning:learning,events:events};document.getElementById("view").innerHTML=f[S.view]();if(S.inspect){var old=document.getElementById("inspect");if(old)old.remove();var d=document.createElement("div");d.id="inspect";d.className="inspector";d.innerHTML='<button class="close" onclick="S.inspect=null;render()">✕</button><h2>'+esc(S.inspect.title)+'</h2><div class="json">'+esc(JSON.stringify(S.inspect.data,null,2))+'</div>';document.body.appendChild(d)}}
function refresh(){Promise.all([get("/api/overview"),get("/api/decisions")]).then(function(x){S.data=x[0];S.data.decisions=x[1];render()}).catch(function(e){document.getElementById("view").innerHTML='<div class="notice">Dashboard backend unavailable: '+esc(e.message)+'</div>'})}
document.querySelectorAll(".nav").forEach(function(b){b.onclick=function(){document.querySelectorAll(".nav").forEach(function(x){x.classList.remove("active")});b.classList.add("active");S.view=b.dataset.view;S.inspect=null;refresh()}});
setInterval(function(){document.getElementById("clock").textContent=new Date().toLocaleTimeString("en-IN",{hour12:false,timeZone:"Asia/Kolkata"})+" IST"},1000);refresh();try{var es=new EventSource("/api/stream");es.addEventListener("telemetry",refresh)}catch(e){}
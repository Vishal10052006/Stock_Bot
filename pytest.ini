[pytest]
# Keep test modules isolated even when different test directories reuse a filename.
# This prevents collection collisions such as tests/journal/test_journal.py vs
# tests/monitoring/test_journal.py while preserving normal repository imports.
addopts = --import-mode=importlib
